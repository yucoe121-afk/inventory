# 프로젝트: 병원 소모품 재고관리 웹앱

**스택:** Next.js + Supabase

**만드는 것:** 병원 소모품 재고관리 웹앱
**누가 쓰나:** 원장 + 간호사/데스크 직원 (코딩 모르는 사람 기준)
**해결하는 문제:** 소모품이 갑자기 떨어지거나, 누가 얼마나 썼는지 모르는 상황을 없앤다

## 작업 규칙

1. **한 번에 한 기능만 작업한다.** 여러 기능을 동시에 벌리지 않는다.
2. **코드를 작성하기 전에 계획을 먼저 제시하고 승인을 받는다.**
3. **기능이 완성되면 세이브(커밋)를 제안한다.** 임의로 커밋하지 않는다.
4. **전문용어는 병원 비유로 설명한다.** (예: "라우팅" → "환자를 접수창구에서 진료실로 안내하는 것", "상태(state)" → "차트에 적힌 현재 정보", "API" → "약국 창구 — 요청하면 정해진 형식으로 응답을 준다")
5. **요청하지 않은 기능을 추가하지 않는다.** MVP 범위 밖의 기능은 아무리 좋아 보여도 만들지 않고, 필요하면 먼저 제안만 한다.
6. **API 키, 시크릿은 반드시 환경변수(.env)로 관리한다.** 코드에 하드코딩하지 않는다.
7. **화면(UI)은 코딩을 모르는 직원이 설명 없이도 바로 쓸 수 있을 만큼 단순하게 만든다.** 전문용어, 숨겨진 메뉴, 다단계 확인 절차를 피한다.

## 이번 버전 범위 (MVP) — 여기 없는 기능은 만들지 않는다

| # | 기능 | 시점 |
|---|------|------|
| ① | 품목 등록/수정/삭제 (품목명, 단위, 최소재고, 분류) | 이번 주 |
| ② | 입고/출고 기록 (품목, 수량, 날짜, 기록자) | 5주차 |
| ③ | 현재고 자동 계산 (입고 합계 − 출고 합계) | 5주차 |
| ④ | 최소재고 미달 품목 빨간 표시 | 5주차 |
| ⑤ | 직원 로그인 | 6주차 |

**성공 기준:** 직원이 설명 없이 입출고를 기록하고, 재고 숫자가 실물과 맞는다.

**데이터 주의:** 환자 정보 없음 (개인정보/의료정보 저장하지 않음)

## v2로 미루는 것 (이번 버전에서 절대 만들지 않음)

- 바코드
- 자동 발주
- 유통기한 관리
- 거래처 관리
- 통계/리포트
